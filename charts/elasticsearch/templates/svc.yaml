apiVersion: v1
kind: Service
metadata:
  name: {{ template "elasticsearch.service.fullname" . }}
  labels:
    app: {{ template "elasticsearch.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    component: {{ .Values.name }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
    role: client
spec:
  type: {{ .Values.serviceType }}
  selector:
    app: {{ template "elasticsearch.name" . }}
    component: {{ .Values.name }}
    role: client
  ports:
  - name: http
    port: 9200
    protocol: TCP

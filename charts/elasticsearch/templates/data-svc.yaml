apiVersion: v1
kind: Service
metadata:
  name: {{ template "elasticsearch.data.service.fullname" . }} 
  labels:
    app: {{ template "elasticsearch.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    component: {{ .Values.name }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
    role: data
spec:
  ports:
  - port: 9300
    name: transport
  clusterIP: None
  selector:
    app: {{ template "elasticsearch.name" . }}
    component: {{ .Values.name }}
    role: data